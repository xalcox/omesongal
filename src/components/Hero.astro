<section
  id="hero"
  class="relative min-h-screen flex items-center justify-center bg-cover bg-center px-6 sm:px-10 animate-zoom-in-slow"
  style="background-image: url('/src/assets/fondo.png');"
>
  <!-- Capa de superposición azul degradada -->
  <div class="absolute inset-0 bg-gradient-to-b from-sky-900/80 to-sky-800/60"></div>

  <!-- Contenido principal centrado -->
  <div class="relative z-10 max-w-4xl mx-auto text-center text-white">
    
    <!-- Icono decorativo -->
    <div class="mb-4 animate-fade-in-up delay-75" aria-hidden="true">
      <svg class="mx-auto h-6 w-6 text-sky-200" fill="currentColor" viewBox="0 0 24 24">
        <path d="M12 2l3 7h7l-5.6 4.6L18.2 22 12 17.8 5.8 22l1.8-8.4L2 9h7z" />
      </svg>
    </div>

    <!-- Título principal del sitio -->
    <h1 class="text-6xl sm:text-6xl lg:text-8xl font-serif font-bold mb-4 animate-fade-in-up">
      O Mesón
    </h1>

    <!-- Subtítulo o claim -->
    <p class="text-sm sm:text-lg italic mb-4 text-sky-200 animate-fade-in-up delay-100">
      Tradición gallega en cada plato
    </p>

    <!-- Descripción principal -->
    <p class="text-base sm:text-xl mb-8 sm:mb-10 max-w-2xl mx-auto animate-fade-in-up delay-200">
      Descubre O Mesón en A Coruña, donde Galicia cobra sabor.
    </p>

    <!-- Botones de acción -->
    <div class="flex flex-col sm:flex-row justify-center gap-3 sm:gap-4 mt-6 animate-fade-in-up delay-400">
      <button
        class="px-6 py-2 text-base sm:text-lg border-2 border-white text-white hover:bg-white hover:text-blue-900 font-medium rounded-md transition-all duration-300"
        aria-label="Reservar mesa"
      >
        Reservar
      </button>

      <a
        href="#menu"
        class="px-6 py-2 text-base sm:text-lg text-white hover:bg-white hover:text-blue-900 border-2 border-transparent hover:border-white font-medium rounded-md transition-all duration-300 text-center"
        aria-label="Ver menú"
      >
        Ver menú
      </a>
    </div>
  </div>
</section>
